# Legacy v1

`legacy-v1` preserves the original `ed2d1c5` application sources and
configuration for historical reference only. It is intentionally excluded
from package discovery, tests, type checks, coverage and Docker build context.

Tracked operating-system and Python bytecode cache files were removed during
migration. The legacy code itself remains unchanged and is not runtime code.
